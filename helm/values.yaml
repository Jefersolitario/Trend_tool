# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

appname: contistreamlitapp
env:
domainEnv:
replicaCount: 1
registry:
  server: energetechnew.azurecr.io
  image: contistreamlitapp
  pullPolicy: Always
  tag:
service:
  type: ClusterIP
  port: 8501
ingress:
  host:
  path: contistreamlitapp
